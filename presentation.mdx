import { themes } from "mdx-deck";
import { CodeSurfer, CodeSurferColumns, Step  } from "code-surfer"
import { dracula, github } from "@code-surfer/themes"
import Images from "./Images.js";
import Intro from "./components/Intro.js";
import Innov8tifHeader from "./components/Innov8tifHeader.js";
import CoolReact from "./components/CoolReact.js"
import ReactLogo from "./components/ReactLogo.js"

export const theme = {
  ...themes.prism,
  ...themes.dracula,
  colors: {
    text: "black",
    background: "white",
    primary: "orange"
  },
   fonts: {
    body: "'Open Sans', ",
  },
  styles: {
    blockquote: {
      fontStyle: 'italic',
      borderLeft: '15px solid orange',
      padding: '30px',
      borderRadius: '30px'
    },
    ul: {
      listStyle: 'none'
    },
    li:{
      marginTop: 12
    }
  }
};

export const absoluteHeader = {    
    position: 'absolute',
    top: '10%',
    bottom: 0,
    zIndex: 1,
    alignSelf: 'center'
  }

export const fullScreenImage = {...absoluteHeader, top: 0, objectFit: "cover", zIndex: 10}

export const zoomableImage = {height: 'auto', width: 'auto' };

<Header>
  <Innov8tifHeader/>
</Header>

<Footer>
  <div style={{marginBottom: 10, fontWeight: '500', fontSize: 14, opacity: .5}}>Osama Qarem</div>
</Footer>





<!-- START -->

<Intro/>

---

# What this talk is

<ul>
  <li>• Drive interest in <CoolReact/> <ReactLogo/></li>
  <li>• Fundamentals overview</li>
  <li>• Live coding</li>
  <li>• Fun learning
    <span style = {{
      position: 'relative',
      bottom: '10px',
      left: '5px',
      fontSize: '40%', 
      width: '100%',
      textAlign: 'center'}}>
      hopefully 😱
      </span>
  </li>
</ul>

<Notes>

  - What is a framework?
  - All cars are similar.
  - React (Electric/Autonomous cars) came along

</Notes>

---

 <img src={Images.jsx} style={{...zoomableImage, minHeight: '70vh'}}/>

 <Notes>

    - The gas pedal is also the brake pedal.

 </Notes>

---

<iframe sandbox="allow-forms allow-scripts" height='500' title='KxPVjm' src='https://insights.stackoverflow.com/survey/2019#technology-_-most-loved-dreaded-and-wanted-web-frameworks' frameBorder='no' allowtransparency='true' allowFullScreen={true} style={{ width: '100%', outline: 'none' }}/>

<Notes>

  - Everyone who drove a Tesla, wants to drive a tesla again.

</Notes>

---

<img src={Images.twitch} style={fullScreenImage}/>

---

<img src={Images.airbnb} style={fullScreenImage}/>

---

<img src={Images.netflix} style={fullScreenImage}/>

---

<img src={Images.bf1} style={{...fullScreenImage, objectFit: 'scale-down'}}/>

<Notes>

- Why are C# developers using React and JS?

</Notes>

---

> # What problems is <CoolReact noUnderline={true}/> solving?

<Notes>

  - Lets start with a bit of history

</Notes>

---

<img src={Images.message} style={{...zoomableImage, maxHeight: '80vh'}} />

<Notes>

  - Its a classic distributed systems problem.

  - Click a message notification. Ops, you already read it.

  - They tried to fix it, it kept coming back.

  - You have 2 independent actors, you interacting with the browser

</Notes>

---

<img src={Images.vdom} style={{...zoomableImage, maxHeight: '80vh'}} />

<Notes>

  - Easily keep UI in sync with application state.

</Notes>

---

<img src={Images.mvc} style={{...zoomableImage, maxHeight: '80vh'}} />


<Notes>

  - MVC allowed views to be talk to models.

  - Debugging is hard.

</Notes>

---

<img src={Images.flux} style={{...zoomableImage, maxHeight: '80vh'}} />

<Notes>


  - 1 way data flow.

  - Debugging is easy.

  - Climbing a tree.

  - You don't need to know this to be productive.

</Notes>

---

# Other 💪🏻

- • Build components — encourages view modularity.
- • State management tools.

---

# Thirsty for some actual code?


### <p><span style={{fontWeight: 'normal'}}>Its time for some </span><s><CoolReact/></s><span style={{fontWeight: 'normal'}}> <b>modern JavaScript 😬</b></span></p>


---

<CodeSurferColumns>

<Step subtitle="First Step">

```js
console.log(1);
console.log(2);
```

```js
console.log("a");
console.log("b");
```

</Step>

<Step subtitle="Second Step">

```js 2
console.log(1);
console.log(2);
```

```js 2
console.log("a");
console.log("b");
```

</Step>

</CodeSurferColumns>

