import { themes } from "mdx-deck";
import { CodeSurfer, CodeSurferColumns, Step  } from "code-surfer"
import { dracula, github } from "@code-surfer/themes"
import Images from "./Images.js";
import Intro from "./components/Intro.js";
import Innov8tifHeader from "./components/Innov8tifHeader.js";
import CoolText from "./components/CoolText.js"
import ReactLogo from "./components/ReactLogo.js"

export const theme = {
  ...themes.prism,
  ...themes.dracula,
  colors: {
    text: "black",
    background: "white",
    primary: "orange"
  },
   fonts: {
    body: "'Open Sans', ",
  },
  styles: {
    blockquote: {
      fontStyle: 'italic',
      borderLeft: '15px solid orange',
      padding: '30px',
      borderRadius: '30px'
    },
    ul: {
      listStyle: 'none'
    },
    li:{
      marginTop: 12
    }
  }
};

export const absoluteHeader = {    
    position: 'absolute',
    top: '10%',
    bottom: 0,
    zIndex: 1,
    alignSelf: 'center'
  }

export const fullScreenImage = {...absoluteHeader, top: 0, objectFit: "cover", zIndex: 10}

export const zoomableImage = {height: 'auto', width: 'auto' };

<Header>
  <Innov8tifHeader/>
</Header>

<Footer>
  <div style={{marginBottom: 10, fontWeight: '500', fontSize: 14, opacity: .5}}>Osama Qarem</div>
</Footer>





<!-- START -->

<Intro/>

---

# What this talk is

<ul>
  <li>• High-level intro</li>
  <li>• Not super technical</li>
  <li>• Drive interest in <CoolText/> <ReactLogo/></li>
  <li>• Fun learning
    <span style = {{
      position: 'relative',
      bottom: '10px',
      left: '5px',
      fontSize: '40%', 
      width: '100%',
      textAlign: 'center'}}>
      hopefully 😱
      </span>
  </li>
</ul>

---

# What is a _framework_?

---

 <img src={Images.jsx} style={{...zoomableImage, minHeight: '70vh'}}/>


---

<iframe sandbox="allow-forms allow-scripts" height='500' title='KxPVjm' src='https://insights.stackoverflow.com/survey/2019#technology-_-most-loved-dreaded-and-wanted-web-frameworks' frameBorder='no' allowtransparency='true' allowFullScreen={true} style={{ width: '100%', outline: 'none' }}/>

<Notes>

  - Everyone who drove a Tesla, wants to drive a tesla again.

</Notes>

---

<img src={Images.twitch} style={fullScreenImage}/>

---

<img src={Images.airbnb} style={fullScreenImage}/>

---

<img src={Images.netflix} style={fullScreenImage}/>

---

<img src={Images.bf1} style={{...fullScreenImage, objectFit: 'scale-down'}}/>

<Notes>

- Why are C# developers using React and JS?

</Notes>

---

> # What problems is <CoolText noUnderline={true}/> solving?

<Notes>

  - Lets start with a bit of history

</Notes>

---

<img src={Images.message} style={{...zoomableImage, maxHeight: '80vh'}} />

<Notes>

  - Its a classic distributed systems problem.

  - Click a message notification. Ops, you already read it.

  - They tried to fix many edge cases, it kept coming back.

  - Something is fundamentally broken

</Notes>

---

<img src={Images.mvc_basic} style={{...zoomableImage, maxHeight: '80vh'}} />

---

<img src={Images.mvc} style={{...zoomableImage, maxHeight: '80vh'}} />


<Notes>

  - Infinite loop.

  - Debugging is hard.

</Notes>

---

# Managing state is hard

---

<img src={Images.flux} style={{...zoomableImage, maxHeight: '80vh'}} />

<Notes>

  - 1 way data flow.

  - Debugging is easy.

  - Climbing a tree.

  - You don't need to know this to be productive.

</Notes>

---

<img src={Images.fun} style={{...zoomableImage, maxHeight: '80vh'}} />

---

<img src={Images.vdom} style={{...zoomableImage, maxHeight: '80vh'}} />

<Notes>

  - Easily keep UI in sync with application state.

</Notes>

---

# <p><span style={{fontWeight: 'normal'}}>Ready for some </span><s><CoolText/></s><span style={{fontWeight: 'normal'}}> <b>modern JavaScript 😬?</b></span></p>

<Notes>

  - Where JS ends and React begins

</Notes>

---

<img src={Images.safebiker} style={{...fullScreenImage, objectFit: "contain"}}/>

___

<img src={Images.unsafebiker} style={{...fullScreenImage, objectFit: "contain"}}/>

---

<div style = {{fontSize: '3rem'}}>

<CodeSurfer >

  ```js title="Declarations"
  var one


  let two


  const three
  ```

</CodeSurfer>

</div>


---


<div style = {{fontSize: '3.5rem'}}>

<CodeSurferColumns>

<Step title="Difference in Scope" >

```js 
function example() {
  
  if(true){
    var i = 123
  }

  console.log(i) 
  
}
```

```js 
function example() {
  
  if(true){
    let i = 123
  }

  console.log(i) 
  
}
```

</Step>

<Step title="Difference in Scope" >

```js 4[5,6,7,8,9],7,8
function example() {
  
  if(true){
    var i = 123
  }

  console.log(i) 
  
}
```

```js 4[5,6,7,8,9],7,8
function example() {
  
  if(true){
    let i = 123
  }

  console.log(i) 
  
}
```

</Step>

<Step title="Difference in Scope" >

```js 4,7,8
function example() {

  if(true){
    var i = 123
  }

  console.log(i) 
  // 123
}
```

```js 4,7,8
function example() {

  if(true){
    let i = 123
  }

  console.log(i) 
  // ReferenceError
}
```

</Step>

<Step title="Difference in Scope" >

```js 3,5 
function example() {

  console.log(i)

  var i = "Hmm..."
}
```

```js 3,5
function example() {

  console.log(x)

  let x = "Hmm..."
}
```

</Step>

<Step title="Difference in Scope" >

```js 3,4,6 
function example() {

  console.log(i)
  // undefined

  var i = "Hmm..."
}
```

```js 3,4,6
function example() {

  console.log(x)
  // ReferenceError

  let x = "Hmm..."
}
```

</Step>

</CodeSurferColumns>

</div>

---
---

<div style = {{fontSize: '3.5rem'}}>

<CodeSurfer >

  ```js title="Object Destructuring" 
  const car = { }

  ```

  ```js title="Object Destructuring" 
  const car = { make: "Honda", model: "Civic", year: "2020" }

  ```

  ```js title="Object Destructuring" 
  const car = { make: "Honda", model: "Civic", year: "2020" }

  const make = car.make
  const model = car.model
  const year = car.year
  ```

  ```js 1,7,8 title="Object Destructuring"
  const car = { make: "Honda", model: "Civic", year: "2020" }

  const make = car.make
  const model = car.model
  const year = car.year

  // Destructured
  const { make, model, year } = car
  ```
  
  ```js title="Array Destructuring" 
  const listOfMonths = [ ]

  ```
  
  ```js title="Array Destructuring" 
  const listOfMonths = [ "March", "April", "May" ]

  ```
  
  ```js title="Array Destructuring" 
  const listOfMonths = [ "March", "April", "May" ]

  const march = listOfMonths[0]
  const april = listOfMonths[1]
  const may = listOfMonths[2]
  ```

  ```js 1,8,9 title="Array Destructuring" 
  const listOfMonths = [ "March", "April", "May" ]

  const march = listOfMonths[0]
  const april = listOfMonths[1]
  const may = listOfMonths[2]

  // Destructured
  const [ march, april, may ] = listOfMonths

  ```


  ```js title="Spread Operator" 
  const car = {
    make: "Nissan",
    model: "Skyline GT-R",
    year: "2020",
    topSpeed: "Slower than Myvi",
  }

  ```

  ```js title="Spread Operator" 
  const car = {
    make: "Nissan",
    model: "Skyline GT-R",
    year: "2020",
    topSpeed: "Slower than Myvi",
  }

  const carClone = {
    make: car.make,
    model: car.model,
    year: car.year,
    topSpeed: car.topSpeed,
  }  
  ```

  ```js title="Spread Operator" 
  const car = {
    make: "Nissan",
    model: "Skyline GT-R",
    year: "2020",
    topSpeed: "Slower than Myvi",
  }

  const carClone = { ...car }  
  ```

  ```js title="Spread Operator" 
  const listOfMonths = [ "March", "April", "May" ]

  const listOfMonthsClone = [ ...listOfMonths ]
  ```

</CodeSurfer >

</div>

---
---

<div style = {{fontSize: '3.5rem'}}>

<CodeSurfer >

```js title="Functions" 
              
  function wow (){

  }

```

```js 1:11 title="First-class Functions" 

  const wow = function (){

  }

```

```js 1:11 title="Higher–Order Functions" 

  const wow = function (){

    return function (){

      
      

    } 
  }

```

```js 1:11 title="Composition" 
  const wow = function (){

    return function (){

      return function (){

      }   
    } 
  }

```

```js 1:11 title="Composition" 
  const wow = function (){

    return function (){

      return function (){

      }   
    } 
  }

```

```js 1:11 title="Composition" 
  const wow = function (){

    return function (){

      return function (){

      }   
    } 
  }

  wow()()()

```

```js title="Arrow Functions" 
  const wow = () => {}

```

```js title="Arrow Functions" 
  const wow = () => () => () => {}

  wow()()()

```

</CodeSurfer >

</div>

---
---

<div style = {{fontSize: '3.5rem'}}>

<CodeSurferColumns>

<Step >

### Function

```jsx
function myFunction (){

  return null
}

```

</Step>

<Step>

#### <CoolText/> Function Component


```jsx 1:5
function Innov8tif (){
  const name = "Innov8tif"

  return <div>{name}</div>
}

```

</Step>

<Step>

#### <CoolText/> Function Component<br/><br/>A function that returns <CoolText text="JSX" noUnderline/>


```jsx 1:5
function Innov8tif (){
  const name = "Innov8tif"

  return <div>{name}</div>
}

```

</Step>

<Step>

#### Use it in <CoolText text="another component" noUnderline/>

```jsx 1:10
function App (){

  return <Innov8tif/>
}

function Innov8tif (){
  const name = "Innov8tif"

  return <div>{name}</div>
}

```

</Step>

<Step>

#### Use it in <CoolText text="another component" noUnderline/>

```jsx 1:10
function App (){

  return <Innov8tif/>
}

function Innov8tif (){
  const name = "Innov8tif"

  return <div>{name}</div>
}

```

</Step>

</CodeSurferColumns>

</div>


---

<video  height="90%" controls>
  <source src={require("./assets/twitter.mp4")} type="video/mp4"/>
</video>

---

# Play with <CoolText/>!

<a href="https://codesandbox.io" target="_blank" style={{marginTop: "5%"}}>
 <img src={Images.codesandbox} style={{...zoomableImage, maxHeight: '50vh'}}/>
</a>

---

# Suggested Resources

- 

---
